from tkinter import *
root = Tk()

# in between is where we right everything that we need for the app
# this the basic frame for using Tkinter (as of now)

root.mainloop() # this line makes sure that the whole app/program does not run out
